# homebrew-tap

Personal [Homebrew](https://brew.sh) tap (casks & formulae).

## Available casks

### Lune

AI-powered terminal with local Ollama suggestions and chat.

```bash
brew install --cask codiku-dev/tap/lune
```

Update:

```bash
brew upgrade --cask lune
```
